/**
 * Cart Thunks
 * Note: Cart operations are synchronous, so these are just action dispatchers
 * For consistency with the architecture pattern
 */

// Cart doesn't need async thunks since all operations are local state updates
// But we keep this file for consistency with the architecture pattern
// All cart actions are handled directly in cartSlice reducers

