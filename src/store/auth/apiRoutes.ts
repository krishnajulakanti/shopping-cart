/**
 * API Routes for Auth
 * Defines all auth-related API endpoints
 */

// Local API routes (through proxy)
export const authRoutes = {
  login: '/auth/login',
  // getUsers: '/users',
  register: '/users',
};

